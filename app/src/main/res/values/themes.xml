<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Rick" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Rick" parent="Base.Theme.Rick" />
    <style name="Theme.RickApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RickApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Theme.Rick.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>